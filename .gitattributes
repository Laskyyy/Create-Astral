# Disable Git line ending conversion, to prevent packwiz index hashes changing when committing from Windows
* -text

# Better highlighting for these files on github
*.packwizignore linguist-language=ignore
*.mcmeta linguist-language=json